// Online C compiler to run C program online
#include <stdio.h>
#include <stdlib.h>
float hesapla(float r)
{
     float pi,area;
     pi=3.14;
     area=pi*r*r;
     return area;
    
}

int main() {
    float sayi;
    
  printf("enter the radius of circle: ");
  scanf("%f", &sayi);
  hesapla(sayi);
  printf("%0.2f",hesapla(sayi));

    return 0;
}
