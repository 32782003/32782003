#include <stdio.h>
 int main ()
 {
   int temp,fp;
   fp=0;
   
   printf("enter the temperature;");
   scanf("%d",&temp);

   if (temp>fp)
   { printf("above the freezing point");
 }
  else if (temp<fp)

  {
    printf("below the freezing point");
  }
     return 0;

   


 }